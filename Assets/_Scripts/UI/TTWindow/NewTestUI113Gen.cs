/// <summary>
/// NewTestUI113 View层 - 自动生成，请勿手动修改
/// </summary>

using UnityEngine;
using UnityEngine.UI;
using TMPro;

public partial class NewTestUI113Gen : MonoBehaviour
{
    public Image ImgA { get; private set; }
    public Image ImgP  { get; private set; }
    public Image ImgPanel { get; private set; }
    public Button BtnPanel { get; private set; }
    public Image ImgFucking { get; private set; }
    public Toggle ToggleFucking { get; private set; }

    private void Awake()
    {
        ImgA = GetComponent<Image>();
        ImgP  = GetComponent<Image>();
        ImgPanel = GetComponent<Image>();
        BtnPanel = GetComponent<Button>();
        ImgFucking = GetComponent<Image>();
        ToggleFucking = GetComponent<Toggle>();
    }
}
